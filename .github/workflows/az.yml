name: Compile Shaders to SPIR-V

on:
  push:
    branches: [ "spv" ]
  pull_request:
    branches: [ "spv" ]
  workflow_dispatch:

jobs:
  compile-shaders:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: mipad/1
          ref: spv
          path: 1

      - name: Install glslang-tools
        run: |
          sudo apt-get update
          sudo apt-get install -y glslang-tools
          glslangValidator --version

      - name: Create output directory
        run: mkdir -p spirv_output

      - name: Compile shaders to SPIR-V
        run: |
          # 递归查找所有 .vert, .frag, .geom, .comp 文件
          find 1 -type f \( -name "*.vert" -o -name "*.frag" -o -name "*.geom" -o -name "*.comp" \) | while read file; do
            rel_path="${file#1/}"
            base="${rel_path%.*}"              # 去掉扩展名
            out_dir="spirv_output/$(dirname "$rel_path")"
            mkdir -p "$out_dir"
            echo "Compiling $file -> $out_dir/${base}.spv"
            glslangValidator -V "$file" -o "$out_dir/${base}.spv"
          done

      - name: List compiled SPIR-V files
        run: find spirv_output -name "*.spv" | sort

      - name: Upload SPIR-V artifacts
        uses: actions/upload-artifact@v4
        with:
          name: spirv-shader-binaries
          path: spirv_output/
