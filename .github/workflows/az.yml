name: Compile All Shaders

on:
  push:
    branches: [ "jiaoben" ]
  pull_request:
    branches: [ "jiaoben" ]
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        if: hashFiles('prepare_*.py') != ''
        with:
          python-version: '3.x'

      - name: Run all prepare scripts
        if: hashFiles('prepare_*.py') != ''
        run: |
          for script in prepare_*.py; do
            echo "Running $script"
            python "$script"
          done

      - name: Install glslang-tools
        run: |
          sudo apt-get update
          sudo apt-get install -y glslang-tools
          glslangValidator --version

      - name: Find and compile all .comp files
        run: |
          shopt -s nullglob
          for comp in *.comp; do
            echo "Compiling $comp -> ${comp%.comp}.spv"
            glslangValidator -V "$comp" -o "${comp%.comp}.spv"
          done

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: compiled-shaders
          path: |
            *.comp
            *.spv
