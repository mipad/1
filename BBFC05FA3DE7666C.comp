#version 450
layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0, std140) uniform support_buffer
{
    uint _m0;
    uint _m1[8];
    vec4 _m2;
    vec4 _m3;
    int _m4;
    float _m5[73];
    ivec4 _m6;
    int _m7;
} support_buffer_1;

layout(set = 0, binding = 1, std140) uniform cp_c0
{
    vec4 _m0[4096];
} cp_c0_1;

layout(set = 1, binding = 0, std430) buffer cp_s0
{
    uint _m0[];
} cp_s0_1;

layout(set = 1, binding = 1, std430) buffer cp_s1
{
    uint _m0[];
} cp_s1_1;

layout(set = 1, binding = 2, std430) buffer cp_s2
{
    uint _m0[];
} cp_s2_1;

layout(set = 1, binding = 3, std430) buffer cp_s3
{
    uint _m0[];
} cp_s3_1;

// 不再使用共享内存
// shared uint _35[2816];

void main()
{
    // 不进行任何复杂计算，直接将所有输出缓冲区写入 0
    // 假设缓冲区至少有足够长度（根据原始代码中使用的最大索引估算）
    // 这里只写前几个元素作为示例，实际可根据需要调整
    
    // 获取调用索引（安全范围）
    uint idx = gl_GlobalInvocationID.x;
    
    // 对 cp_s3 写入 0（原始代码最后的输出位置）
    // 使用安全索引（最多写入 65535，避免越界）
    if (idx < 65535u) {
        cp_s3_1._m0[idx] = 0u;
    }
    
    // 如果需要保留输出语义，也可以写入其他缓冲区
    // 但为了避免崩溃，只写入一个已知安全的缓冲区
}